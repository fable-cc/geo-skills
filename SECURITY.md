# 安全策略

## 报告漏洞

如果您发现安全漏洞，请通过 GitHub 的 [Security Advisory](https://github.com/fable-cc/geo-skills/security/advisories) 报告。

## 依赖

本项目依赖以下第三方库：

- `openai`（可选）— 用于调用 OpenAI API
- `requests`（可选）— 用于 HTTP 请求
- `mkdocs`（可选）— 用于文档构建

所有依赖均为纯 Python，无二进制扩展。建议定期检查依赖更新。
